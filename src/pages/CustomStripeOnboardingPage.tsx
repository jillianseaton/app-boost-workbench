
import React from 'react';
import CustomStripeOnboarding from '@/components/CustomStripeOnboarding';

const CustomStripeOnboardingPage = () => {
  return <CustomStripeOnboarding />;
};

export default CustomStripeOnboardingPage;
